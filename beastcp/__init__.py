"""
beastcp: High-performance file copying utility with resume, verification,
tqdm progress, and multi-threaded directory synchronization.
"""

__version__ = "1.0.2"

# Optional: expose CLI function if someone imports the library
from .cli import cli
